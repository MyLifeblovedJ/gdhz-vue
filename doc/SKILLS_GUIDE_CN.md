# 🧠 AI 技能使用与管理指南

> 本文档面向全栈开发人员（Java 后端为主）、项目/产品经理角色，涵盖所有已安装技能的分类说明、使用场景和维护方法。

---

## 📍 安装信息

| 项目 | 说明 |
|------|------|
| **安装路径** | `C:\Users\Administrator\.agent\skills` |
| **技能目录** | `.agent/skills/skills/`（每个子目录一个技能） |
| **技能总数** | 703 个社区技能 + 1 个独立技能（Interface Design） |
| **来源** | [antigravity-awesome-skills](https://github.com/sickn33/antigravity-awesome-skills) + [interface-design](https://github.com/Dammyjay93/interface-design) |

---

## 🔄 更新方式

### 社区技能仓库

```bash
git -C "$HOME/.agent/skills" pull
```

### Interface Design（独立安装）

```bash
# 克隆最新版到临时目录
git clone --depth 1 https://github.com/Dammyjay93/interface-design.git /tmp/interface-design-update

# 覆盖复制核心技能文件
cp -r /tmp/interface-design-update/.claude/skills/interface-design/* "$HOME/.agent/skills/skills/interface-design/"

# 清理临时目录
rm -rf /tmp/interface-design-update
```

---

## 📂 技能分类总览

```
┌──────────────────────────────────────────────┐
│              AI 技能按角色分类                  │
├──────────┬───────────┬───────────┬────────────┤
│ 后端架构  │  前端/UI   │  测试/质量  │ 产品/项目   │
│ 安全开发  │  数据分析   │  运维部署   │ 文档/内容   │
└──────────┴───────────┴───────────┴────────────┘
```

---

## 1️⃣ 必备入门（Essentials）

> 🎯 **适用人群**：所有人。建议每次开发都激活。

| 技能 | 功能 | 使用场景 |
|------|------|----------|
| `concise-planning` | 任务规划 | 开始任何功能开发前，先制定计划 |
| `lint-and-validate` | 代码质量检查 | 提交代码前自动检查代码规范 |
| `git-pushing` | Git 安全操作 | 需要提交/推送代码时 |
| `kaizen` | 持续改进 | 代码 Review 或重构时，识别改进点 |
| `systematic-debugging` | 系统化调试 | 遇到难以定位的 Bug 时 |

### 使用示例

```
"使用 @concise-planning 帮我规划用户认证模块的开发计划"
"用 @systematic-debugging 帮我排查这个 NullPointerException"
"用 @lint-and-validate 检查我修改的代码"
```

---

## 2️⃣ 后端架构（Architecture & Design）

> 🎯 **适用人群**：后端架构师、Java 开发。系统设计和架构决策时使用。

| 技能 | 功能 | 使用场景 |
|------|------|----------|
| `senior-architect` | 全面的软件架构指导 | 设计新系统、评审架构时 |
| `architecture-patterns` | 架构模式（DDD/六边形/Clean） | 选择或实施架构模式时 |
| `microservices-patterns` | 微服务拆分与通信 | 微服务设计、服务间通信方案时 |
| `event-sourcing-architect` | 事件溯源与 CQRS | 设计事件驱动系统时 |
| `architecture-decision-records` | ADR 决策文档 | 记录重要技术决策（为什么选 A 不选 B） |
| `java-pro` | Java 语言高级特性 | Java 日常开发、性能优化 |

### 使用示例

```
"用 @senior-architect 审查我的订单系统架构"
"用 @architecture-patterns 帮我分析这个模块应该用 DDD 还是传统分层"
"用 @java-pro 帮我优化这段 Java 代码的性能"
```

---

## 3️⃣ 全栈开发（Full-Stack Developer）

> 🎯 **适用人群**：需要同时处理前后端的开发者。

| 技能 | 功能 | 使用场景 |
|------|------|----------|
| `senior-fullstack` | 全栈开发完整指南 | 端到端功能开发 |
| `frontend-developer` | React/Next.js 前端开发 | Vue/React 前端页面开发 |
| `api-patterns` | REST/GraphQL/tRPC 选择 | 设计 API 接口时 |
| `database-design` | 数据库设计与 ORM | 数据表设计和 SQL 优化 |
| `backend-dev-guidelines` | 后端开发模式 | 后端服务编码规范 |

### 使用示例

```
"用 @senior-fullstack 帮我做一个完整的用户管理功能"
"用 @api-patterns 帮我设计这组 RESTful API"
"用 @database-design 审查我的数据库表结构"
```

---

## 4️⃣ 前端 — 网络向导（Web Wizard）

> 🎯 **适用人群**：需要构建高性能 Web 应用的开发者。

| 技能 | 功能 | 使用场景 |
|------|------|----------|
| `frontend-design` | UI 设计准则与美学 | 页面整体布局和视觉设计 |
| `react-best-practices` | React 性能优化 | React 组件性能问题 |
| `react-patterns` | 现代 React 模式 | 组件设计和状态管理 |
| `nextjs-best-practices` | Next.js 最佳实践 | Next.js 项目开发 |
| `tailwind-patterns` | Tailwind CSS 样式 | 使用 Tailwind 做样式时 |
| `form-cro` | 表单转化优化 | 表单设计和用户体验优化 |
| `seo-audit` | SEO 审计 | 网站 SEO 检查和优化 |

---

## 5️⃣ 前端 — 网页设计师（Web Designer）

> 🎯 **适用人群**：追求像素级完美体验的前端开发者。

| 技能 | 功能 | 使用场景 |
|------|------|----------|
| `ui-ux-pro-max` | 高级设计系统和令牌 | 建立完整设计规范 |
| `frontend-design` | UI 美学基础层 | 页面视觉风格定义 |
| `3d-web-experience` | Three.js / R3F 3D 效果 | 可视化、3D 大屏展示 |
| `canvas-design` | 静态视觉和海报 | 数据可视化画布、图表 |
| `mobile-design` | 移动端设计 | 响应式和移动优先设计 |
| `scroll-experience` | 滚动驱动体验 | 滚动动画和交互效果 |

---

## 6️⃣ 🌟 Interface Design（界面设计工程）— 重点介绍

> 🎯 **专为应用界面设计**：仪表盘、管理面板、SaaS 应用、工具页面、数据界面。
> ⚠️ **不适用于**：营销着陆页、推广网站（这些应使用 `frontend-design`）。

### 核心理念

Interface Design 不是一个简单的 UI 模板工具，而是一套**设计工程方法论**，解决 AI 生成界面时的「千篇一律」问题。

#### 三大支柱

1. **Craft（工艺）** — 每个设计选择都必须有理由，而非使用默认值
2. **Memory（记忆）** — 设计决策跨会话保持一致（通过 `system.md`）
3. **Consistency（一致性）** — 系统化的设计令牌和规范

### 工作流程

```
┌─────────────────────────────────────────────────────┐
│  1. 意图确认（Intent First）                          │
│     → 这个人是谁？要完成什么？应该有什么感觉？            │
├─────────────────────────────────────────────────────┤
│  2. 产品域探索（Domain Exploration）                   │
│     → 领域概念、色彩世界、标志性元素、要避免的默认值       │
├─────────────────────────────────────────────────────┤
│  3. 方向提案 → 用户确认                                │
├─────────────────────────────────────────────────────┤
│  4. 组件构建（每个组件前必须声明设计检查点）              │
├─────────────────────────────────────────────────────┤
│  5. 质量检查（4项强制检查）                             │
│     → 替换测试 / 模糊测试 / 签名测试 / 令牌测试          │
├─────────────────────────────────────────────────────┤
│  6. 保存模式到 .interface-design/system.md              │
└─────────────────────────────────────────────────────┘
```

### 组件设计检查点（每次编写 UI 前必须声明）

```
Intent:   [目标用户是谁、要做什么、应该什么感觉]
Palette:  [从产品域探索中选的颜色 — 为什么适合]
Depth:    [边框/阴影/分层 — 为什么这样选]
Surfaces: [层级体系 — 为什么这个色温]
Typography: [字体 — 为什么适合这个意图]
Spacing:  [基础间距单位]
```

### 设计原则速查

| 原则 | 要点 |
|------|------|
| **微妙分层** | 表面层级通过极细微的明度变化（而非颜色变化）建立 |
| **边框** | 用 `rgba` 低透明度，而非实色 hex — 边框应「感觉到但看不见」 |
| **侧边栏** | 与主内容 **同色** + 分隔线，而非不同背景色 |
| **输入框** | 比周围 **稍暗**（内凹感），而非更亮 |
| **令牌命名** | `--ink`、`--parchment` 而非 `--gray-700` — 令牌应能猜出产品 |
| **色彩** | 一个重点色用好，胜过五个无目的的颜色 |
| **圆角** | 统一尺度：小（输入框）→ 中（卡片）→ 大（模态框） |

### 可用命令

| 命令 | 用途 |
|------|------|
| `/interface-design:status` | 查看当前设计系统状态 |
| `/interface-design:audit` | 检查代码是否符合设计系统 |
| `/interface-design:extract` | 从现有代码中提取设计模式 |
| `/interface-design:critique` | 对已构建的界面进行工艺批评，重建有默认值的部分 |
| `/interface-design:init` | 初始化项目的设计系统 |

### 典型使用场景

| 场景 | 怎么用 |
|------|--------|
| **新建仪表盘** | 激活技能，先做产品域探索，再逐步构建 |
| **重构现有管理后台 UI** | 用 `extract` 提取现有模式，用 `critique` 评估质量 |
| **保持多页面设计一致性** | 用 `system.md` 存储设计决策，每次会话自动加载 |
| **数据可视化大屏** | 先确定 Intent（谁在什么环境看），再选色彩世界 |
| **GIS 态势感知系统** | 在产品域中探索海洋/气象概念，建立专属的设计签名 |

### 使用示例

```
"用 @interface-design 帮我设计一个海洋灾害态势感知仪表盘"
"用 @interface-design:critique 审查右侧面板的设计质量"
"用 @interface-design:extract 从现有代码中提取设计令牌"
"用 @interface-design:audit 检查所有组件是否遵循设计系统"
```

### 📘 开发实战教程

以下是在日常开发中使用 Interface Design 的 **5 个典型场景**和具体对话示例。

#### 场景一：从零开始设计新页面

当你要开发一个新页面（比如新增一个「海况监测」页面），在对话中这样说：

```
帮我设计一个海况监测页面的 UI，使用 @interface-design 技能。
这个页面需要展示实时潮位、波高和风速数据。
```

AI 会按照 Interface Design 的流程自动执行：

1. **先做产品域探索** — 提出 5+ 个领域概念、色彩世界、签名元素
2. **提出设计方向** — 而不是直接写代码
3. **等你确认方向** — "这个方向对吗？"
4. **再动手写代码** — 每个组件都带设计检查点

#### 场景二：审查和优化现有界面

当你觉得现有页面看起来「一般般」或「不够专业」：

```
用 @interface-design 审查一下 RightSidebar.vue 的设计，帮我找出可以优化的地方。
```

或者使用专用命令做更精确的操作：

| 命令 | 用途 | 示例 |
|------|------|------|
| `/interface-design:critique` | 工艺批评 | 找出使用了默认值的地方并重建 |
| `/interface-design:audit` | 合规检查 | 检查代码是否遵循设计系统 |
| `/interface-design:extract` | 提取模式 | 从已有代码中提取设计令牌 |

#### 场景三：保持设计一致性（跨会话记忆）

完成一个页面设计后，AI 会问你是否保存设计模式。同意后它会在项目中创建 `.interface-design/system.md`，记录：

- 设计方向和感觉（如「深海指挥中心」）
- 深度策略（边框/阴影/分层的选择理由）
- 间距基准（如 4px base unit）
- 关键组件模式和令牌

**下次开发新页面时**，AI 自动读取此文件，确保新旧页面风格一致。

#### 场景四：日常组件开发

即使只是加一个小组件，也可以按设计规范来：

```
帮我在右侧边栏加一个风速数据卡片，遵循 @interface-design 的设计原则。
```

AI 会在写代码前先声明设计检查点：

```
Intent:     值班人员快速查看风速趋势，感觉：紧凑、专业
Palette:    --accent-cyan 冰蓝色强调异常值
Depth:      subtle border（rgba 低透明度）
Surfaces:   --bg-card 深色面板内嵌
Typography: Rajdhani 数字 + Noto Sans SC 标签
Spacing:    8px 基准
```

然后才动手编写代码。

#### 场景五：初始化新项目的设计系统

在一个全新项目开始时：

```
/interface-design:init
```

AI 会引导你完成完整的设计系统初始化，包括产品域探索、色彩定义、令牌架构、组件模式等。

### 📋 命令速查

| 你说什么 | AI 做什么 |
|---------|----------|
| `@interface-design` + 设计需求 | 走完整流程（探索→提案→确认→构建→检查） |
| `/interface-design:init` | 初始化项目设计系统 |
| `/interface-design:critique` | 批评界面工艺质量 |
| `/interface-design:audit` | 检查代码是否符合设计规范 |
| `/interface-design:extract` | 从代码中提取设计模式 |
| `/interface-design:status` | 查看当前设计系统状态 |

> [!IMPORTANT]
> **Interface Design vs Frontend Design 适用范围**
>
> - `@interface-design` → 仪表盘、管理面板、SaaS 应用、数据大屏（**本项目适用**）
> - `@frontend-design` → 营销着陆页、推广网站、品牌展示页

---

## 7️⃣ 安全开发（Security Developer）

> 🎯 **适用人群**：需要构建安全应用的开发者。代码安全审计和加固时使用。

| 技能 | 功能 | 使用场景 |
|------|------|----------|
| `api-security-best-practices` | API 安全设计模式 | 设计和审查 API 接口安全性 |
| `auth-implementation-patterns` | JWT/OAuth2/Session 认证 | 实现用户认证和授权 |
| `backend-security-coder` | 后端安全编码 | Java/Python 后端安全加固 |
| `frontend-security-coder` | 前端安全（XSS 防护） | 前端输入过滤和 XSS 防护 |
| `cc-skill-security-review` | 安全检查清单 | 新功能上线前安全审查 |
| `pci-compliance` | 支付卡安全标准 | 涉及支付功能时的合规检查 |

### 使用示例

```
"用 @api-security-best-practices 审查我的 API 端点安全性"
"用 @auth-implementation-patterns 帮我实现 JWT 认证"
"用 @backend-security-coder 检查这个 Java 服务的安全漏洞"
```

---

## 8️⃣ 数据与分析（Data & Analytics）

> 🎯 **适用人群**：需要处理数据可视化、SQL 优化、数据库设计的开发者。

| 技能 | 功能 | 使用场景 |
|------|------|----------|
| `analytics-tracking` | GA4/PostHog 埋点 | 设置数据分析和用户行为追踪 |
| `claude-d3js-skill` | D3.js 数据可视化 | 自定义图表和数据大屏 |
| `sql-pro` | 现代 SQL 与云数据库 | SQL 编写和优化 |
| `postgres-best-practices` | PostgreSQL 优化 | PG 数据库性能调优 |
| `ab-test-setup` | A/B 测试设计 | 功能实验验证 |
| `database-architect` | 数据库架构设计 | 从零设计数据库架构 |

### 使用示例

```
"用 @sql-pro 优化这个慢查询"
"用 @database-architect 帮我设计新模块的数据库表"
"用 @claude-d3js-skill 帮我做一个潮位趋势可视化图表"
```

---

## 9️⃣ 测试与质量（QA & Testing）

> 🎯 **适用人群**：需要保证代码质量和进行自动化测试的开发者/测试人员。

| 技能 | 功能 | 使用场景 |
|------|------|----------|
| `test-driven-development` | TDD 红→绿→重构 | 新功能开发采用 TDD 流程 |
| `systematic-debugging` | 系统化调试 | 复杂 Bug 排查 |
| `browser-automation` | Playwright E2E 测试 | 自动化浏览器测试 |
| `e2e-testing-patterns` | E2E 测试套件设计 | 建立可靠的端到端测试 |
| `code-review-checklist` | PR 代码审查清单 | 代码 Review 时的检查项 |
| `test-fixing` | 系统化修复失败测试 | 测试失败时的排查流程 |

### 使用示例

```
"用 @test-driven-development 帮我用 TDD 开发这个功能"
"用 @code-review-checklist 审查这个 PR"
"用 @browser-automation 帮我写一个登录流程的 E2E 测试"
```

---

## 🔟 产品与项目管理（Product & Business）

> 🎯 **适用人群**：项目经理、产品经理。产品规划、竞品分析、商业分析时使用。

| 技能 | 功能 | 使用场景 |
|------|------|----------|
| `product-manager-toolkit` | RICE 优先级排序、PRD 模板 | 编写产品需求文档、需求排序 |
| `competitive-landscape` | 竞品分析 | 调研竞争对手 |
| `competitor-alternatives` | 对比页面生成 | 创建产品对比文档 |
| `business-analyst` | 数据驱动的分析与 KPI | 商业分析和指标设计 |
| `launch-strategy` | 产品发布策略 | 产品上线计划 |
| `copywriting` | 营销文案 | 编写产品描述和宣传文案 |

### 使用示例

```
"用 @product-manager-toolkit 帮我写一份灾害预警功能的 PRD"
"用 @competitive-landscape 分析市面上的海洋监测系统"
"用 @business-analyst 帮我设计这个项目的 KPI 指标体系"
```

---

## 1️⃣1️⃣ 运维与部署（DevOps & Cloud）

> 🎯 **适用人群**：需要容器化部署、CI/CD 的开发运维人员。

| 技能 | 功能 | 使用场景 |
|------|------|----------|
| `docker-expert` | Docker 容器与多阶段构建 | 容器化应用、编写 Dockerfile |
| `deployment-procedures` | 安全发布策略 | 上线部署和回滚策略 |
| `bash-linux` | 终端命令行 | Shell 脚本编写 |
| `environment-setup-guide` | 环境标准化 | 团队开发环境搭建 |

### 使用示例

```
"用 @docker-expert 帮我写一个 Spring Boot 应用的 Dockerfile"
"用 @deployment-procedures 帮我制定上线部署计划"
```

---

## 1️⃣2️⃣ 文档与内容（Creative & Content）

> 🎯 **适用人群**：需要编写技术文档、用户文档的人员。

| 技能 | 功能 | 使用场景 |
|------|------|----------|
| `content-creator` | SEO 文档内容 | 撰写技术博客、产品文档 |
| `copy-editing` | 文案润色 | 优化已有文档的表达 |
| `copywriting` | 营销文案 | 产品宣传资料 |

### 使用示例

```
"用 @content-creator 帮我写一篇技术博客"
"用 @copy-editing 润色这份接口文档"
```

---

## 🧭 场景速查表

> 快速找到适合当前工作的技能。

| 我要做什么 | 用什么技能 |
|-----------|-----------|
| 开始一个新功能 | `@concise-planning` → `@senior-fullstack` |
| 设计系统架构 | `@senior-architect` + `@architecture-patterns` |
| 设计数据库 | `@database-design` 或 `@database-architect` |
| 写 Java 代码 | `@java-pro` |
| 设计 UI 界面（仪表盘/管理面板） | `@interface-design` |
| 设计 UI 界面（营销/落地页） | `@frontend-design` + `@ui-ux-pro-max` |
| API 接口设计 | `@api-patterns` + `@api-security-best-practices` |
| 实现登录认证 | `@auth-implementation-patterns` |
| 安全审查 | `@cc-skill-security-review` + `@backend-security-coder` |
| 写 SQL / 优化查询 | `@sql-pro` + `@postgres-best-practices` |
| 数据可视化 | `@claude-d3js-skill` + `@canvas-design` |
| 调试难题 | `@systematic-debugging` |
| 写测试 | `@test-driven-development` + `@browser-automation` |
| 代码审查 | `@code-review-checklist` |
| 写 PRD | `@product-manager-toolkit` |
| 竞品分析 | `@competitive-landscape` |
| Docker 部署 | `@docker-expert` + `@deployment-procedures` |
| 写文档 | `@content-creator` + `@copy-editing` |
| Git 提交/推送 | `@git-pushing` |

---

## ⚠️ 注意事项

1. **技能是按需加载的** — 你不需要记住所有 703 个技能，AI 会根据上下文自动匹配相关技能
2. **用 `@` 前缀显式调用** — 如果想确保使用特定技能，用 `@技能名` 即可
3. **Interface Design 与 Frontend Design 的区别**：
   - `interface-design`：仪表盘、管理面板、SaaS 应用等**功能性界面**
   - `frontend-design`：营销页面、着陆页等**展示性界面**
4. **Symlink 限制** — Windows 上的 `docx`、`pdf`、`pptx`、`xlsx` 处理技能因 symlink 权限未安装，如需使用请以管理员身份修复
