# Home Legend And Landing Design

> 日期：2026-03-11
> 范围：`apps/gdhz-vue-2999`

## 目标

- 修复首页图例消失问题
- 保持图例下沿与左侧 `智能决策` 卡片下沿对齐
- 参考 3000 端口态势感知首页台风组件的布局骨架，简化 2999 台风专题摘要区，只保留 `预计登陆`

## 设计结论

- 图例回到稳定的 `top` 定位模式，不再依赖 `bottom` 定位切换
- 通过测量 `智能决策` 卡片与图例自身高度，计算图例 `top`，实现下沿对齐
- 台风专题删除 `移速移向` 卡，只保留一个满宽的 `预计登陆` 卡，内部维持主值 + 次值结构

## 验收标准

- 图例重新出现且稳定可见
- 图例下沿与 `智能决策` 卡片外边框底边对齐
- 台风专题摘要区仅保留 `预计登陆`，布局参考 3000 端口的简洁结构
